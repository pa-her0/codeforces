#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<pair<int,int>>a(n+1);
    for(int i=0;i<n;i++){
        int x,y;
        cin >> x >>y;
        a[i].first=x;a[i].second=y;
    }

    
    for (int i=0;i<n;i++){

    }

    return 0;
}